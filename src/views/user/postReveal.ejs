<!DOCTYPE html>
<html lang="en">
  <%- include('../partials/head') %>
<body>
  <%- include('../partials/navbar') %>
  <div class="wrapper">
    <div class="post-reveal">
      <div class="header">
        <h2><%= post.title %></h2>
      </div>
      
      <p><%= post.description %></p>
      <p><%= post.content %></p>
      
      <hr>
      <div class="footer-post-reveal">
        <p>Categoria: <%= post.category.name_category %></p>
        <p>Slug: <%= post.category.slug %></p>
        <small><%= post.date %></small>
      </div>
    </div>
  </div>
</body>
</html>